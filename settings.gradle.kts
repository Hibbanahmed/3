pluginManagement {
    repositories {
        google()
        mavenCentral()
        jcenter() // Note: jcenter() is deprecated, consider using other repositories.
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
        jcenter() // Note: jcenter() is deprecated, consider using other repositories.
    }
}